## 1. 陪跑领域模型和状态机

- [x] 1.1 新增陪跑会话数据模型，包含目标文本、目标类型、时长、强度、开始时间、结束时间、提醒次数、最近状态和状态持续时间
- [x] 1.2 新增强度配置表，定义轻、标准、严格的偏离阈值、卡住阈值和本轮最大提醒次数
- [x] 1.3 新增陪跑状态枚举，覆盖 `on_track`、`distracted`、`stuck`、`milestone`、`unclear`
- [x] 1.4 实现提醒判定函数，根据状态、置信度、持续时间、强度阈值和提醒上限决定是否介入
- [x] 1.5 为状态机和提醒判定补最小单元测试

## 2. 陪跑 AI 协议

- [x] 2.1 新增目标感知 prompt，输入目标文本、目标类型、强度、最近状态、最近提醒和当前截图
- [x] 2.2 新增结构化 JSON 解析，读取 `state`、`confidence`、`screen_summary`、`target_relevance`、`should_interrupt`、`message`、`suggested_action`
- [x] 2.3 对无效 JSON、未知状态、低置信度和空消息做兜底处理，默认进入 `unclear` 且不通知
- [x] 2.4 增加主动消息校验，拒绝没有观察、目标关系或下一步动作的纯吐槽
- [x] 2.5 用样例响应覆盖 `on_track`、`distracted`、`stuck`、`milestone`、`unclear` 的解析测试

## 3. 菜单栏和陪跑设置窗口

- [x] 3.1 在托盘菜单新增「开始陪跑...」入口，并通过 UI 队列通知主进程
- [x] 3.2 新增陪跑设置窗口，支持填写本轮目标、选择目标类型、时长和强度
- [x] 3.3 目标为空时阻止开始，并在窗口内显示中文提示
- [x] 3.4 陪跑中更新菜单状态，显示目标摘要、剩余时间和「结束陪跑」入口
- [x] 3.5 点击「结束陪跑」时结束当前会话并触发总结流程

## 4. 主循环接入

- [x] 4.1 将当前随机评论流程改为非陪跑状态默认安静，保留主动聊天能力
- [x] 4.2 陪跑中每轮截图后调用目标感知分析协议
- [x] 4.3 沿用 dHash 去重，重复画面不重复调用 AI，但继续累计当前状态持续时间
- [x] 4.4 根据提醒判定函数控制通知，达到强度上限后只记录状态不打扰
- [x] 4.5 陪跑结束时生成本轮总结，并将总结推送到通知队列和聊天历史

## 5. 持久化和历史展示

- [x] 5.1 扩展 SQLite schema，支持记录陪跑开始、提醒、状态快照、结束和总结事件
- [x] 5.2 写入陪跑开始事件，包含目标文本、目标类型、时长和强度
- [x] 5.3 写入陪跑提醒事件，包含状态、截图概括、目标关系、提醒内容和建议动作
- [x] 5.4 写入陪跑总结事件，包含本轮目标、主要推进、偏离或卡住片段和下一步建议
- [x] 5.5 聊天窗口按时间顺序展示当天普通对话、陪跑提醒和陪跑总结

## 6. 验证

- [x] 6.1 语法检查通过：`PYTHONPYCACHEPREFIX=/private/tmp/screenchat-pycache python3 -m compileall -q src`
- [x] 6.2 单元测试覆盖陪跑状态机、强度阈值、AI JSON 解析和消息校验
- [ ] 6.3 手工验证菜单栏可打开陪跑设置窗口，并能创建标准强度陪跑
- [ ] 6.4 手工验证非陪跑状态不再弹随机吐槽通知
- [ ] 6.5 手工验证偏离、卡住和结束总结三类通知均包含下一步动作
- [ ] 6.6 手工验证陪跑事件和总结写入 SQLite，并能在聊天窗口看到
